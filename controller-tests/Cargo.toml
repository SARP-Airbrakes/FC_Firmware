[package]
name = "controller-tests"
version = "0.1.0"
description = "This crate is used for processing past flight data with the different controller subcomponents for testing."
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
controller = { version = "0.1.0", path = "../controller" }
csv = "1.4.0"
env_logger = "0.11.11"
indicatif = "0.18.6"
indicatif-log-bridge = "0.2.3"
log = "0.4.34"
