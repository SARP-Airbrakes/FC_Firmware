[package]
name = "w25qxxxjv"
version = "0.1.0"
edition = "2024"

[dependencies]
defmt = { version = "1.1.1", optional = true }
embedded-hal = "1.0.0"
embedded-hal-async = "1.0.0"

[features]
defmt = ["dep:defmt"]
